#enable ufw
sudo ufw enable

#allow incoming traffics on ports 80 and 8080
sudo ufw allow 80/tcp
sudo ufw allow 8080/tcp

#forwarding rules
*nat
:PREROUTING ACCEPT [0:0]
-A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80
COMMIT

#reload ufw
udo ufw reload

